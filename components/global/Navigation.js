import { storyblokEditable, StoryblokComponent } from "@storyblok/react";

const Navigation = ({ blok }) => {
  return (
    <nav className="navbar navbar-light sticky-top border-bottom">
      <div className="container">
          <img src="/fghr-logo-svg.svg" width="50" height="50" alt="Fund for Global Human Rights text logo resembling several orange bricks." />
      </div>
    </nav>

  );
};

export default Navigation;
