import { storyblokEditable, StoryblokComponent } from "@storyblok/react";

const Navigation = ({ blok }) => {
  return (
    <nav className="navbar navbar-light pt-4">
      <div className="container">
          <img src="/fghr-logo-svg.svg" width="60" height="60" alt="Fund for Global Human Rights text logo resembling several orange bricks." />
      </div>
    </nav>

  );
};

export default Navigation;
